

const SectionTitle = ({title}) => {
    return (
        <div>
            <p className="uppercase text-red-500 font-semibold">-----{title}</p>
        </div>
    );
};

export default SectionTitle;